I getMove algo: Brug ikke et map (x, y) -> Piece, men i stedet (x, y) -> int, hvor int betegner sort eller hvid. Piece er bare sugar for Game og GUI klasserne.
White = 0
Black = 1
WhiteKing = 2
BlackKing = 3


Current bugs and TODOs:

Tillad at capture flere i én tur.
Man SKAL tage en brik hvis man kan.
Clean isValidMoveMen



Man skal tage hvis man kan:
game.getValidMovesForPlayer() -> alle lovlige moves lige nu. Hvis det ikke er ens tur returneres None.
                                 skal tage hensyn til at hvis man KAN tage en brik, SKAL man gøre det.
                                 derfor, hvis et sådan træk findes, er alle andre ulovlige.



        # Must be players turn
        if pieceFrom.color in {Constants.Constants.WhiteMen, Constants.Constants.WhiteKing}:
            if not self.playerInTurn == Constants.Constants.WhitePlayer:
                return False
        elif pieceFrom.color in {Constants.Constants.BlackMen, Constants.Constants.BlackKing}:
            if not self.playerInTurn == Constants.Constants.BlackPlayer:
                return False